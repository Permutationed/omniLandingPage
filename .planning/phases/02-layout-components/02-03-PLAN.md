---
phase: 02-layout-components
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/layout/footer-socials.tsx
  - components/layout/footer.tsx
autonomous: true

must_haves:
  truths:
    - "User sees footer with logo and tagline"
    - "User sees navigation links in footer"
    - "User sees legal placeholder links (Privacy Policy, Terms of Service)"
    - "User sees social media icons (Twitter, LinkedIn, Instagram)"
  artifacts:
    - path: "components/layout/footer-socials.tsx"
      provides: "Social icons using react-social-icons"
      exports: ["FooterSocials"]
    - path: "components/layout/footer.tsx"
      provides: "Complete footer component"
      exports: ["Footer"]
  key_links:
    - from: "components/layout/footer.tsx"
      to: "components/layout/footer-socials.tsx"
      via: "FooterSocials import"
      pattern: "<FooterSocials"
---

<objective>
Build the footer component with logo, navigation links, legal placeholders, and social media icons.

Purpose: Footer provides secondary navigation and legal/social links, completing the page structure. Social icons use react-social-icons library for consistent brand icons without deprecated lucide-react brand icons.

Output: FooterSocials client component and Footer server component.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-layout-components/02-RESEARCH.md

# From Plan 01 (dependencies)
# react-social-icons is installed
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FooterSocials component</name>
  <files>components/layout/footer-socials.tsx</files>
  <action>
    Create `components/layout/footer-socials.tsx`:
    - 'use client' directive (SocialIcon uses hooks internally)
    - Import SocialIcon from 'react-social-icons'

    Social links array (placeholder URLs that can be updated later):
    ```typescript
    const socialLinks = [
      { url: 'https://twitter.com/omnicard', label: 'Twitter' },
      { url: 'https://linkedin.com/company/omnicard', label: 'LinkedIn' },
      { url: 'https://instagram.com/omnicard', label: 'Instagram' },
    ]
    ```

    Implementation:
    - Flex container with items-center gap-3
    - Map over socialLinks, render SocialIcon for each
    - SocialIcon props:
      - url={social.url}
      - target="_blank"
      - rel="noopener noreferrer"
      - aria-label={social.label}
      - style={{ width: 32, height: 32 }}
      - bgColor="transparent"
      - fgColor="currentColor"
      - className="text-muted-foreground hover:text-foreground transition-colors"

    NOTE: Use twitter.com URL (not x.com) for react-social-icons to correctly detect the Twitter/X icon.
  </action>
  <verify>
    - File exists at components/layout/footer-socials.tsx
    - `npx tsc --noEmit` passes
  </verify>
  <done>FooterSocials renders three social icons with proper styling and accessibility</done>
</task>

<task type="auto">
  <name>Task 2: Create Footer component</name>
  <files>components/layout/footer.tsx</files>
  <action>
    Create `components/layout/footer.tsx` (server component - no 'use client'):
    - Import Link from 'next/link'
    - Import FooterSocials from './footer-socials'

    Define footer link sections:
    ```typescript
    const footerLinks = {
      nav: [
        { href: '#how-it-works', label: 'How It Works' },
        { href: '#features', label: 'Features' },
        { href: '#faq', label: 'FAQ' },
      ],
      legal: [
        { href: '/privacy', label: 'Privacy Policy' },
        { href: '/terms', label: 'Terms of Service' },
      ],
    }
    ```

    Layout structure:
    - footer element with border-t bg-muted/30
    - Container with py-12 md:py-16
    - Grid: gap-8 md:grid-cols-4
      - Col 1-2 (md:col-span-2): Logo + tagline
        - Logo: Link to "/" with "Omni Card" text (font-bold text-xl)
        - Tagline: "Earn rewards on your biggest expense. Pay tuition with your credit card." (text-sm text-muted-foreground max-w-xs mt-2)
      - Col 3: Navigation section
        - h3 "Navigation" (font-medium mb-4)
        - ul with nav links (text-sm text-muted-foreground hover:text-foreground)
        - Nav links are anchor tags (for smooth scroll), not Next.js Links
      - Col 4: Legal section
        - h3 "Legal" (font-medium mb-4)
        - ul with legal links using Next.js Link component (these are actual pages)
    - Bottom bar: mt-12 pt-8 border-t flex flex-col md:flex-row justify-between items-center gap-4
      - Copyright: "YYYY Omni Card. All rights reserved." (use dynamic year via {new Date().getFullYear()})
      - FooterSocials component
  </action>
  <verify>
    - File exists at components/layout/footer.tsx
    - `npx tsc --noEmit` passes
  </verify>
  <done>Footer renders complete footer structure with logo, tagline, nav links, legal links, copyright, and social icons</done>
</task>

</tasks>

<verification>
Run after all tasks complete:
1. `npx tsc --noEmit` - no TypeScript errors
2. Both files exist:
   - components/layout/footer-socials.tsx
   - components/layout/footer.tsx
</verification>

<success_criteria>
- FooterSocials renders Twitter, LinkedIn, Instagram icons with transparent background and currentColor fill
- Icons have proper accessibility (aria-label, target="_blank", rel="noopener noreferrer")
- Footer has 4-column grid layout on desktop, stacked on mobile
- Logo links to home, tagline describes value proposition
- Navigation links point to page sections (anchor hrefs)
- Legal links use Next.js Link for /privacy and /terms routes
- Copyright shows current year dynamically
- Social icons render in bottom bar
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-components/02-03-SUMMARY.md`
</output>
